<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Audio: needed to record background noise during focus sessions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Internet: needed to download the generated avatar from the APIs -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Vibration: needed for haptic feedback during key moments -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Calendar: needed to read events -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- Service Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Gallery Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Camera Permission -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Significant Motion Permission -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindBricks">

        <!-- Launcher activity -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.MindBricks">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Remove ActionBar -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.MindBricks.NoActionBar" />

        <!-- Onboarding activity without ActionBar -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:theme="@style/Theme.MindBricks.NoActionBar" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:label="@string/settings_title"
            android:theme="@style/Theme.MindBricks.NoActionBar" />

        <service
            android:name=".service.SensorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone|specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="sensors" />
        </service>

        <!-- FileProvider for camera photos -->
        <!-- Source: https://developer.android.com/training/secure-file-sharing/setup-sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>